/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["prod-ripcut-delivery.disney-plus.net", "daisyui.com"],
    formats: ["image/webp"],
  },
};

module.exports = nextConfig;
